//Copyright 2012, etc.

(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(['jquery', 'underscore', 'backbone', 'react', 'react.backbone'], factory);
    } else {
        // Browser globals
        root.Clinical = factory(root.$, root._, root.Backbone, root.React, root.ReactBB);
    }
}(this, function ($, _, Backbone, React, ReactBB) {
