{
  "name": "@oberd/oui",
  "version": "0.0.1",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "playwright test",
    "prepublish": "npm run build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.4",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "fuse.js": "^7.1.0"
  }
}
