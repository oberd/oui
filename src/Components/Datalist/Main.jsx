/*global define */
define(function (require) {
  'use strict';
  var React = require('react.backbone');
  var Datalist = React.createClass({
    render: function () {
      return (
        <div>
          Datalist
        </div>
      );
    }
  });
  return Datalist;
});
