{
    "name": "covid-safe",
    "version": "1.0.0",
    "description": "example application using oberd oui",
    "main": "index.js",
    "scripts": {
        "lint": "eslint --ignore-path .gitignore \"**/*.{ts,js}\" && npm run check-format",
        "format": "prettier --write . --config ../../.prettierrc --ignore-path .gitignore",
        "check-format": "prettier --check . --config ../../.prettierrc --ignore-path .gitignore",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@oberd/oui-inputs": "workspace:*"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "vite": "^2.4.1"
    }
}
